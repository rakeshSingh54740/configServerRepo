# 🏥 Clinic Service
clinic:
  spring:
    config:
      activate:
        on-profile: clinic-dev
    application:
      name: clinic-service
  server:
    port: 8091
    servlet:
      context-path: /clinic
