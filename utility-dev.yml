# 🧰 Utility Service
utility:
  spring:
    config:
      activate:
        on-profile: utility-dev
    mvc:
      throw-exception-if-no-handler-found: true
    web:
      resources:
        add-mappings: false
  server:
    port: 8095

